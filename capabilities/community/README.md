# Community Capability Packages

Use this folder for capability JSON packages intended to be shared or reviewed
for broader use. Keep packages data-only and compatible with the schema in
`docs/capabilities/package-schema.md`.
