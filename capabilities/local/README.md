# Local Capability Packages

Put checkout-local capability JSON files here. These files are source packages;
use `./script/capability_package.sh install-dev-dir capabilities/local` to copy
validated packages into ASTRA Dev's channel-local capability library.
