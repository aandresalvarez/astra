enum RuntimeGuardrailsPresentation {
    static let hostPrivacyTitle = "ASTRA App Privacy Boundary"
    static let hostPrivacyStatus = "Always On"
    static let hostPrivacySystemImage = "hand.raised.fill"
    static let hostPrivacyDetail =
        "ASTRA blocks implicit scans of Photos, Music, app bundles, Mail, Messages, media libraries, and external volumes. Sandboxed agent subprocesses also hard-deny those roots unless the user explicitly selects them as workspace/input paths."
}
